using System.Windows;

namespace PersonalAccountant
{
    /// <summary>
    /// Interaction logic for ProfitWindow.xaml
    /// </summary>
    public partial class ProfitWindow : Window
    {
        public ProfitWindow()
        {
            InitializeComponent();
        }

        private void AddProfitBtn_Click(object sender, RoutedEventArgs e)
        {
           //decimal n;
           //bool isNumeric = decimal.TryParse(ProfitTB.Text, out n);
           //
           //if (isNumeric)
           //{
           //    Accountant.AddProfit(DescriptionTB.Text, ProfitTB.Text);
           //    this.Close();
           //}
        }
    }
}
